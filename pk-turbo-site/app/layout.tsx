import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ 
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'PK Turbo LLC - Quick, Rapid, Turbo Delivery Service',
  description: 'PK Turbo LLC provides reliable, fast, and efficient transportation and delivery services throughout the region.',
  keywords: 'transportation, delivery, logistics, trucking, shipping, freight, cargo, PK Turbo, New York',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="flex flex-col min-h-screen">
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
